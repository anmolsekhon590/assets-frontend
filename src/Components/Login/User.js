// This class models the user 
// with 2 data members - password and username
export class User {
    constructor(username, password) {
        this.username = username;
        this.password = password;
    }
}